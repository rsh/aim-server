# Retro AIM Server Configuration

# Disable password check and auto-create users at login
# Uncomment this in development environments for easier testing
# WARNING: Do NOT enable this in production - it's a security risk!
# DISABLE_AUTH=true

# Logging level: trace, debug, info, warn, error
LOG_LEVEL=info

# Chat subdomain for SSL/TLS certificate configuration
# Used to generate stunnel.conf for encrypted OSCAR/TOC connections
# Example: chat.yourdomain.com
# If not set, setup.sh will prompt you to enter it
# CHAT_DOMAIN=chat.example.com
